#include <iostream>

using namespace std;

int main(){
  std::cout << "Hello world." << std::endl;
  std::cout << "add the line which is not necessary." << std::endl;
  
  std::cout << "Add the function in the branches." << std::endl;
  while(std::cin.get() != '\n')
  return 0;
}
